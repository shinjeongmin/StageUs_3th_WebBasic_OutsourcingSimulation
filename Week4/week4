함수형 프로그래밍의 목표

함수만 사용한다고 함수형 프로그래밍은 아님.

1. 순수 함수
- 캡슐화?
부작용 : 함수 내의 동작의 실행으로 함수 외에 영향을 미치는 것.
- 함수의 결과로 다른 함수에 영향을 주면 안됨
- 같은 input에 대해서는 항상 같은 return을 내보내야 함

2. 불변성
- 매개변수로 받은 값이 변경되면 안됨.

3. 선언형
- 선언형 <-> 명령형
- 명령형 : how / 선언형 : what